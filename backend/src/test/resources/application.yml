server:
  port: 8083

spring:
  datasource:
    url: jdbc:h2:mem:A708;MODE=MYSQL;
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
      show_sql: true
    open-in-view: false